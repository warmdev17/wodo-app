version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries"
    schema: "db/migrations"
    gen:
      go:
        package: "repositories"
        out: "internal/repositories"
        emit_interface: true
        emit_json_tags: true
